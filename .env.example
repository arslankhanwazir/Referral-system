# PostgreSQL connection string (Supabase, Neon, RDS, local, etc.)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/referral_db"

# App URL, used to build the referral link shown on the dashboard
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Never commit the real .env file. Commit .env.example only.
